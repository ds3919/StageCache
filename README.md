# External-Storage-Program-Execution-Performance-Boost
Manages 'hot' files being used for any given program stored on an external storage device and optimizes their runtime location to reduce stuttering caused by CPU stalling while fetching new data from a bottlenecked connection.
